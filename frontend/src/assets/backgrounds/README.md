# Backgrounds

Metti qui gli sfondi scaricati da internet, per esempio:

```text
src/assets/backgrounds/login-bg.jpg
```

Poi importali in React:

```jsx
import loginBg from '../assets/backgrounds/login-bg.jpg'
```

Oppure usali nel CSS solo se sono dentro `public/`.
